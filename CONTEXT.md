# calpeek

Mac のカレンダーを覗き、ステータスバー(tmux・sketchybar 等)に埋め込める一行の予定表示を出力する CLI。関心は「今日これから何があるか」のみ。

## Language

**判定対象イベント**:
now / next の候補になりうるイベント。「時刻指定があり、自分が辞退しておらず、対象カレンダー上にある」ものを指す。終日イベントと辞退済みイベントは存在しないものとして扱う。

**now(進行中の予定)**:
現在時刻が開始〜終了の間にある判定対象イベント。複数重なっている場合は最も遅く始まったもの(同時開始なら終了が早いもの)を 1 件選ぶ。
_Avoid_: 現在のイベント、current

**next(次の予定)**:
現在時刻より後に始まる当日の最も早い判定対象イベント。進行中の予定と時間が重なっていても候補になる。当日中に存在しなければ next はない。
_Avoid_: 直近の予定、upcoming

**終日イベント**:
時刻指定のないイベント。判定対象外であり、now にも next にも一切ならない。

**対象カレンダー**:
判定対象イベントの探索先。既定では Mac 上の全カレンダー。利用者が名前で絞り込むことができる。

**30分ルール**:
now が存在し、かつ「next の開始時刻 − 現在時刻 ≤ 30分」のとき、now と next の 2 件を表示する規則。基準は常に現在時刻から next 開始までの残り時間であり、now の終了時刻は関係しない。

## 表示の決定表

| now あり | next あり | next まで ≤30分 | 出力 |
|---|---|---|---|
| ✓ | ✓ | ✓ | now → next の 2 件 |
| ✓ | ✓ | ✗ | now のみ |
| ✓ | ✗ | — | now のみ |
| ✗ | ✓ | — | next のみ |
| ✗ | ✗ | — | 何も表示しない(空) |

## Example dialogue

> **Dev**: 14:00〜15:00 の全社会の最中に 14:30〜14:45 の 1on1 がある。14:35 の now はどっち?
>
> **Domain expert**: 1on1。now は「最も遅く始まったもの」だから。そして 14:35 時点の next は当日のそれ以降で最初に始まる予定。もしそれが 15:00 開始なら残り 25 分なので 30分ルールが発動し、1on1 → その予定の 2 件表示になる。
>
> **Dev**: 今日が終日の有給イベントだけの日は?
>
> **Domain expert**: 終日イベントは存在しないものとして扱う。now も next もない状態になる。
